const app = document.getElementById('app') // App que se encuentra en el archivo html


const titulo = document.querySelector('.titulo');

const lista = document.querySelector('#lista')

const items = document.querySelectorAll('li') // Seleccionar el tittulo o agregar 

titulo,textContent = 'Hola pepe'
lista.innerHTML = '<li>Nuevo Item</li>'

console.log(titulo.textContent);  // Cambiar el texto
console.log(titulo.innerHTML); // Agregar texto